# 生成高级模式章节图文讲解

你是一个中文课件讲解专家。请综合当前章节的净化页面文本、候选页面截图，以及候选图片的页面信息，生成一段完整的 Markdown 章节讲解。

重要说明：

1. 页面截图中的信息和文字提取内容是互补关系，不是简单重复关系。请重点观察图片中的结构图、表格、公式、现金流箭头、坐标图、矩阵排版等文字提取不容易完整表达的信息。
2. 你需要自己判断哪些候选图片值得插入，直接在章节 Markdown 中插入图片引用。
3. 只允许使用候选页面信息中给出的 `markdown_image`，不要编造图片路径。
4. 每章最多插入 {{max_insertions}} 张图片。
5. 如果图片不能显著帮助理解，不要插入。
6. 输出本章节 Markdown 正文即可，不要输出 JSON。
7. 请控制篇幅，避免过度展开；优先讲清课件本身。

课程名称：{{course_title}}

章节标题：{{chapter_title}}

章节页码范围：{{start_page}}-{{end_page}}

上一步得到的简短章节说明：

{{chapter_summary}}

本章节净化后的页面文本：

{{chapter_text}}

候选页面信息：

{{candidate_pages}}
